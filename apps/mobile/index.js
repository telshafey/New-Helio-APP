import { AppRegistry } from 'react-native';
import App from './App';
// This should match the name in app.json, but since we don't have that file, we'll use a default.
const appName = 'HelioMobile';

AppRegistry.registerComponent(appName, () => App);
